export type items = {
  id: number;
  img: string;
  title: string;
  price: number;
  choice: boolean;
  new: boolean;
  size: string[];
};

export type log = {
  username: string;
  password: string;
};
